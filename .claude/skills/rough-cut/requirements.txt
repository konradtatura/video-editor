# CPU-only PyTorch. Install this line separately first with the CPU index,
# then install the rest -- otherwise pip may pull the much larger CUDA build:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# then:
#   pip install -r requirements.txt
faster-whisper
numpy
scipy
groq
transformers  # ctc_backend.py -- second-opinion repeat detector (wav2vec2 CTC), independent of Whisper's decoder bias
